# ModelSelectionusing_cross_val_Score.ipynb
This Python code uses sklearn.model_selection's cross_val_score to run cross validations and taking out mean score to finalise the best algorithm and model.
